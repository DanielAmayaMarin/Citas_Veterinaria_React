
const Error = ({children}) => {
  return (
    <section className="bg-red-800 text-white text-center p-3 uppercase font-bold mb-3 rounded-md">
            {children}
    </section>
  )
}

export default Error